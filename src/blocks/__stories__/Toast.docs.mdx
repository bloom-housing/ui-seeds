import { Meta } from "@storybook/addon-docs"
import { ArgTypes, Title, Subtitle, Description } from "@storybook/blocks"
import CommonMessage from "../shared/CommonMessage"
import Toast from "../Toast"
import * as stories from "./Toast.stories.tsx"

<Meta of={stories} />

<Title />
<Subtitle>
  Toast components are brief notifications that appear at the top of the screen for a short
  duration, typically used to provide feedback, notification, or confirmation.
</Subtitle>
Read more documentation about the Toast component in
[Zeroheight](https://zeroheight.com/5e69dd4e1/p/99531d-toast).

## Properties

<ArgTypes of={CommonMessage} exclude={["role", "closeable"]} />

## Theme Variables

| Name                        | Description                   | Default                        |
| --------------------------- | ----------------------------- | ------------------------------ |
| `--toast-padding`           | Interior spacing              | `--seeds-s4`                   |
| `--toast-border-radius`     | Radius                        | `0`                            |
| `--toast-border`            | Border shorthand              | `none`                         |
| `--toast-flex-gap`          | Space between icons/text      | `--seeds-s3`                   |
| `--toast-icon-scale-factor` | Integer                       | `1`                            |
| `--toast-max-width`         | Size                          | `--seeds-width-sm`             |
| `--toast-font`              | Font                          | `--seeds-font-sans`            |
| `--toast-font-size`         | Component font size           | `--seeds-type-caption-size`    |
| `--toast-link-gap`          | Space between text and a link | `--seeds-s1`                   |
| `--toast-link-font-weight`  | Font weight                   | `--seeds-font-weight-semibold` |

In addition, there is a set of variables you can customize via `#toast-stack` which affects the fixed container within which toasts are rendered:

| Name                        | Description                      | Default      |
| --------------------------- | -------------------------------- | ------------ |
| `--toast-stack-gap`         | Spacing between toasts           | `--seeds-s4` |
| `--toast-stack-inset-top`   | Window top edge -> stack space   | `--seeds-s4` |
| `--toast-stack-inset-right` | Window right edge -> stack space | `--seeds-s4` |
| `--toast-stack-width`       | Size                             | `auto`       |
