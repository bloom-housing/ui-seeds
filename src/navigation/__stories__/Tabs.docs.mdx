import { Meta } from "@storybook/addon-docs"
import { ArgTypes, Title, Subtitle, Description } from "@storybook/blocks"
import { Swatch } from "../../../documentation/components/Swatch.tsx"
import Tabs from "../Tabs"
import * as stories from "./Tabs.stories.tsx"

<Meta of={stories} />

<Title />
<Subtitle>
  Tabs allows users to navigate between multiple sections of content within a single container, or
  present tab styling for URL-based navigation.
</Subtitle>
Read more documentation about the Tabs component in
[Zeroheight](https://zeroheight.com/5e69dd4e1/p/99dc98-tabs).

## Properties

<ArgTypes of={Tabs} />

## Tab List Properties

<ArgTypes of={Tabs.TabList} />

## Tab Properties

<ArgTypes of={Tabs.Tab} />

## Tab Panel Properties

<ArgTypes of={Tabs.TabPanel} />

## Navigation Mode

Use `navigation` when you want tab styling for URL-based navigation without tab-panel behavior. In this mode,
each `Tabs.Tab` can receive an `href` and optional `active` state, and clicking a tab
navigates to a URL. You can also pass `navigationLabel` to set the accessible name
for the `<nav>` landmark (defaults to `Section navigation`).

```tsx
<Tabs navigation>
  <Tabs.TabList>
    <Tabs.Tab href="/listings" active>
      Listings
    </Tabs.Tab>
    <Tabs.Tab href="/applications">Applications</Tabs.Tab>
    <Tabs.Tab href="/settings">Settings</Tabs.Tab>
  </Tabs.TabList>
</Tabs>
```

## Theme Variables

| Name                           | Description                                             | Default                        |
| ------------------------------ | ------------------------------------------------------- | ------------------------------ |
| `--tab-font`                   | Font family                                             | `--seeds-font-alt-sans`        |
| `--tab-font-size`              | Font size                                               | `--seeds-type-body-size`       |
| `--tab-font-size-sm`           | Font size on mobile screens                             | `--seeds-type-label-size`      |
| `--tab-font-weight`            | Font weight                                             | `--seeds-font-weight-semibold` |
| `--tab-font-weight-sm`         | Font weight on mobile screens                           | `--seeds-font-weight-regular`  |
| `--tab-background-color`       | <Swatch color="bloom-bg-color-surface" border={true} /> | `--seeds-bg-color-surface`     |
| `--tab-label-color`            | <Swatch color="bloom-text-color-light" border={true} /> | `--seeds-text-color-light`     |
| `--tab-padding-inline`         | Horizontal spacing within tab interior                  | `--seeds-s6`                   |
| `--tab-padding-block`          | Vertical spacing within tab interior                    | `--seeds-s4`                   |
| `--tab-padding-inline-sm`      | Horizontal spacing on mobile screens                    | `--seeds-s6`                   |
| `--tab-padding-block-sm`       | Vertical spacing on mobile screens                      | `--seeds-s3`                   |
| `--tab-border-radius`          | Tab border radius                                       | `--seeds-rounded-lg`           |
| `--tab-border-color`           | <Swatch color="bloom-border-color" />                   | `--seeds-border-color`         |
| `--tab-border-width`           | Tab border radius                                       | `--seeds-border-1`             |
| `--tab-active-label-color`     | <Swatch color="bloom-color-on-surface" />               | `--seeds-color-on-surface`     |
| `--tab-active-indicator-width` | Width of the colorful border                            | `--seeds-border-2`             |
| `--tab-active-indicator-color` | <Swatch color="bloom-color-primary" />                  | `--seeds-color-primary`        |
| `--tab-panel-padding`          | Spacing within the panel interior                       | `--seeds-spacer-section`       |
| `--tab-panel-background-color` | <Swatch color="bloom-bg-color-surface" border={true} /> | `--seeds-bg-color-surface`     |
| `--tab-panel-border-color`     | <Swatch color="bloom-border-color" border={true} />     | `--seeds-border-color`         |
| `--tab-panel-border-width`     | Tab border radius                                       | `--seeds-border-1`             |
| `--tab-vertical-sidebar-width` | Width of the sidebar in vertical layout for desktop     | `--seeds-s60`                  |
| `--tab-collapse-breakpoint`    | Screen size breakpoint for vertical orientation         | `--sm-only`                    |
