import { Meta } from "@storybook/addon-docs"
import { ArgTypes, Title, Subtitle, Description } from "@storybook/blocks"
import CheckboxGroup from "../CheckboxGroup"
import * as stories from "./CheckboxGroup.stories.tsx"

<Meta of={stories} />

<Title />
<Subtitle>
  CheckboxGroup components are a wrapper around a native multiselect checkbox input element set
  which styles the checkboxes as Seeds buttons.
</Subtitle>

## Properties

<ArgTypes of={CheckboxGroup} />

## Theme Variables

| Name                                | Description            | Default      |
| ----------------------------------- | ---------------------- | ------------ |
| `--checkbox-group-inner-button-gap` | Space between elements | `--seeds-s3` |
