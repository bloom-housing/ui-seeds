import { Meta } from "@storybook/addon-docs"
import { ArgTypes, Title, Subtitle, Description } from "@storybook/blocks"
import { Swatch } from "../../../documentation/components/Swatch.tsx"
import Grid from "../Grid"
import * as stories from "./Grid.stories.tsx"

<Meta of={stories} />

<Title />
<Subtitle>
  Grid components are a CSS grid-based layout system for creating a responsive, flexible grid of
  columns.
</Subtitle>
Read more documentation about the Grid component in
[Zeroheight](https://zeroheight.com/5e69dd4e1/p/76600d-grid).

## Properties

<ArgTypes of={Grid} />

## Row Properties

<ArgTypes of={Grid.Row} />

## Cell Properties

<ArgTypes of={Grid.Cell} />

## Theme Variables

| Name                | Description | Default         |
| ------------------- | ----------- | --------------- |
| `--grid-gap-sm`     |             | `--seeds-s4`    |
| `--grid-gap-md`     |             | `--seeds-s6`    |
| `--grid-gap-lg`     |             | `--seeds-s8`    |
| `--grid-gap-xl`     |             | `--seeds-s12`   |
| `--grid-gap-mobile` |             | `--grid-gap-sm` |
| `--grid-gap`        |             | `--grid-gap-md` |
